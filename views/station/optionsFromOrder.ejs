<% if(options) { %>
<div class="container">
    <div class="row row-cols-2">
        <% for(var i=0; i< options.length; i++) { %>
        <div class="col">
            <span><%=options[i].name %></span>
        </div>
        <% } %>
    </div>
</div>
<% } else { %>
<span>Keine</span>
<% } %>